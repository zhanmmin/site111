# 发布记录与内容映射复查

- 审查日期：2026-08-05
- 复查范围：发布时间、内容库记录、公开链接、编辑弹窗、发布流程、图片上传与预览、指南页、登录入口、刷新后的状态保持，以及买家公开页映射。
- 结论：发布时间和内容对应关系已补齐，发布流程已收敛到“编辑与发布”，新增指南和登录入口，刷新、编辑和公开页回归通过。

## 步骤与结果

1. 复查原页面：内容库原先显示“更新于 刚刚”，无法知道准确发布时间；记录仅显示短 ID。— 发现问题，见 `01-before-publish-time.png`。
2. 发布更新：记录现在保存 `publishedAt`，显示本地时间 `YYYY-MM-DD HH:mm:ss`；点击“发布更新”会更新时间。— 已修复。
3. 内容对应关系：每条记录显示标题、模式、价格、具体发布时间、完整公开链接，并标记当前编辑内容。— 已修复，见 `03-content-library-after.png`。
4. 编辑与公开页：编辑弹窗读取同一条记录，公开页使用同一内容 ID，标题、价格、模式和链接一致。— 通过。
5. 刷新与错误回归：刷新后时间、ID、公开链接仍保持对应；后台和买家页浏览器错误日志为空。— 通过。

## 标注复查与调整

1. “查看公开页面”和“发布更新”不再放在页面标题区，避免两个动作含义不清；页面标题改为“编辑与发布”。— 已调整。
2. “发布内容”移动到买家预览旁的“发布前编辑”区域，符合发布前确认的操作顺序。— 已调整，见 `04-editor-actions.png`。
3. “打开”保留在每条已发布内容的操作区，和该条内容的标题、价格、时间、公开链接同一行管理。— 已调整。
4. “使用指南”改为可切换的完整指南页面，包含创建、发布、订单、收款和发布前检查清单。— 已调整，见 `05-guide-page.png`。
5. 顶部创作者入口改为登录弹窗，登录后进入设置页；演示环境只保存登录状态，不保存密码。— 已调整，见 `06-login-dialog.png`。
6. 图片内容新增主图上传、双图上传、6 MB 校验、原图压缩和自动生成加密预览；未上传时阻止发布。— 已调整，见 `07-image-upload.png`。
7. 移除管理员编辑区内重复的买家预览卡、CONTENT EDITOR / 发布前编辑标题和付款前状态提示，保留轻量创建内容入口及右侧访问、价格、链接设置。— 已调整，见 `08-preview-cleanup.png`。

## 证据截图

- `01-before-publish-time.png`：修复前页面状态。
- `02-content-library-before.png`：修复前内容库区域的审查记录。
- `03-content-library-after.png`：修复后显示具体发布时间、完整链接和“当前编辑”。
- `04-editor-actions.png`：编辑与发布页及发布内容按钮的新位置。
- `05-guide-page.png`：完整使用指南页面。
- `06-login-dialog.png`：创作者登录入口和登录弹窗。
- `07-image-upload.png`：图片内容的上传入口、预览生成提示和未上传校验。
- `08-preview-cleanup.png`：精简后的发布工作区，保留发布入口和必要配置。

## 兼容说明

旧内容没有真实时间字段时，会在首次迁移时补充一个可追踪的时间；新建内容、编辑保存和发布更新都会写入真实发布时间。当前项目仍是前端演示数据，真实后端发布记录需要接入服务端数据库。
